---
- name: Copy file to remote clients
  hosts: all
  vars:
   srcfile: /home/somesh/somefile

  tasks:
  - name: Copying file
    become: true
    copy:
     src: "{{ srcfile }}"
     dest: /tmp
     owner: somesh
     group: somesh
     mode: 0644
